#include <iostream>
#include <cmath>
using namespace std;

int main() {
            int x,y;
            cout<<"Enter the number ";
            cin>>x;
            cout<<"\nEnter the exponent ";
            cin>>y;
            cout<<"\n The value of "<<x<<"^"<<y<<" is "<<pow (x,y);
	return 0;
}
